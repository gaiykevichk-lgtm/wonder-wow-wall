"""Concrete `FileStorage` implementations.

Phase 6 ships only `LocalFileStorage`; the S3 adapter is tracked in
`docs/design-docs/FILE-STORAGE-ROADMAP.md`. Both will conform to the
domain-layer ABC `app.domain.media.services.FileStorage`.
"""
