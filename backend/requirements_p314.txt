# Web framework
fastapi==0.115.12
uvicorn[standard]==0.34.3

# Database
sqlalchemy[asyncio]==2.0.41
asyncpg==0.31.0
alembic==1.16.2

# Auth & security
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.20
slowapi==0.1.9

# Validation
pydantic==2.11.4
pydantic-settings==2.9.1
email-validator==2.2.0

# Testing
pytest==8.4.1
pytest-asyncio==1.0.0
httpx==0.28.1
# aiosqlite — used by tests/infrastructure/test_alembic.py to apply migrations
# against an in-memory SQLite DB without a postgres server. Production still
# uses asyncpg + postgres (see DATABASE_URL).
aiosqlite==0.20.0

# Redis (optional, for caching)
redis==6.2.0

# Image validation (Phase 6 admin uploads). Pillow re-decodes the file to
# verify it's a real image and to read pixel dimensions — see
# `app/application/media/use_cases.py`. 10.x is the current stable line
# with security backports; pin major to avoid surprise breakage on minor
# upgrades that occasionally rename PIL.Image API.
Pillow>=11.3.0
