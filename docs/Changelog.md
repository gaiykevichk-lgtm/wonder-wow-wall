# Changelog — Wonder Wow Wall

История создания и доработки продукта.
Записи ведутся в обратном хронологическом порядке (новое сверху).

---

## 24.04.2026

### Фоторедактор стен: честная перспектива и автоопределение

Большой этап работы над перспективой стены на фото: от срочного фикса визуальной регрессии до авто-определения углов и калибровки. Выкатывалось в несколько независимых релизов.

**Исправление визуальной регрессии:**
- Панели в режиме перспективы снова показывают реальный дизайн с искажением под углом стены — ранее на их месте отображалась сплошная цветная заливка без текстуры

**Настоящий mesh-warp для перспективы:**
- Каждая панель в перспективе рендерится как искажённый quad через сетку 8×8 треугольников с кусочно-аффинным преобразованием — края ровные, без швов и алиасинга
- Добавлен кэш warped-изображений: повторный рендер той же панели в той же перспективе мгновенный
- Подстройка яркости панели под освещение стены перенесена внутрь warp-рендерера — корректнее работает с цветными оттенками

**Связка калибровки с раскладкой в перспективе:**
- Размеры панелей в перспективе теперь действительно соответствуют физическим сантиметрам: панель 30×30 см при калибровке через розетку имеет правильный размер в любом углу стены
- Авто-заполнение стены учитывает перспективу: проверка «помещается ли панель» идёт по quad-форме, а не по bounding-box — меньше ложных отказов у границ стены
- Авто-заполнение блокируется, если калибровка не выполнена — вместо расстановки «на глаз» показывается подсказка откалибровать масштаб

**Авто-определение перспективы:**
- При загрузке фото система автоматически ищет прямые линии (плинтусы, карнизы, стыки потолка) и на их основе вычисляет углы перспективы стены
- Если уверенность распознавания высокая — углы подставляются автоматически, пользователю остаётся только подтвердить или подправить
- Если линий мало (пустая однотонная стена) — показывается подсказка отметить углы вручную
- Баннер «Перспектива определена автоматически» с кнопкой «Сбросить» для отката к ручному режиму

**Авто-определение масштаба по эталону:**
- На фото автоматически детектируются ориентиры известного размера (розетка ~8,5 см, выключатель, плинтус) и по ним рассчитывается пикселей на сантиметр
- Пользователь видит список найденных эталонов и может переключиться между ними
- Если эталон не найден — автокалибровка пропускается, остаётся ручной режим

**Сохранение на сервере:**
- Углы перспективы и параметры калибровки теперь сохраняются в проекте на бэкенде — при повторном открытии проекта они восстанавливаются точно как были
- Отдельные эндпоинты частичного обновления перспективы и калибровки: drag угла → debounce ~1 с → PATCH без пересылки всего проекта
- Оптимистичная блокировка по версии: если в двух вкладках одновременно редактировать один проект, вторая получит понятное сообщение «данные изменились, обновите страницу» вместо тихого затирания

**Инфраструктура:**
- В проект добавлены миграции БД (Alembic) — теперь изменения схемы накатываются и откатываются одной командой вместо ручной правки PostgreSQL
- Заложен фундамент для авто-перспективы на пустых стенах через анализ глубины ML-моделью на бэкенде (ядро готово, подключение к пользовательскому интерфейсу — следующий этап)

**Стабилизация после код-ревью:**
- По итогам аудитов каждой из фаз закрыто более 40 пунктов технического долга: согласованы имена, устранены дубликаты, добавлена защита от гонок между вкладками и потоками, расширено покрытие тестами
- Добавлены тесты для всех новых модулей: mesh-warp математики, quad-coverage, детекторов перспективы и эталонов, персистентности углов на бэкенде, частичных PATCH-эндпоинтов

**Авто-перспектива: backend-first для надёжного определения стены:**
- Основной путь распознавания стены переключён с клиентского OpenCV на серверный depth+RANSAC — работает устойчиво даже на однотонных стенах без видимых линий (плинтусов, карнизов), где Hough-детектор ничего не находил
- Новый inline-эндпоинт принимает фото и маску стены прямо в теле запроса — распознавание срабатывает **сразу после загрузки фото**, до сохранения проекта; раньше приходилось сначала сохранять проект, чтобы запустить фоновый анализ
- OpenCV-распознавание остаётся как резервный путь (offline-режим, недоступность сервера) — 11 МБ WASM-бандла больше не грузится на критическом пути первого показа
- Устранён «вечный» спиннер «Определяем углы стены…»: гарантированный переход в готовое состояние в течение 15 секунд независимо от сетевых и рантайм-ошибок
- Backend возвращает также габариты стены в пикселях — фронтенд использует их для подстановки плейсхолдер-калибровки (3 м по ширине как безопасное значение для показа масштабной шкалы); честный режим: авто-заполнение панелями по-прежнему требует ручной двухточечной калибровки

**Тесты нового пайплайна:**
- Backend: happy-path для inline-эндпоинта, требование авторизации, валидация размеров маски, error-envelope для недоступного depth-бэкенда, форма ответа с bbox_pixels
- Frontend: сериализация snake_case в теле запроса, маппинг 422/plane_fit_failed и 503/depth_unavailable в типизированную ошибку, дефолтные значения bbox при обратной совместимости

**Панели теперь ложатся по перспективе стены автоматически:**
- Раньше при быстрой загрузке фото и клике «Заполнить стену» плитки ложились плоской сеткой, даже когда чуть позже приходил результат распознавания перспективы — панели уже были размещены в photo-space без искажения
- Теперь авто-калибровка из детектированной ширины стены заменяет upload-плейсхолдер и помечается как доверенная (маркер `wallWidthCm`), что снимает блок `AutoFillBlockedError` для перспективного авто-заполнения
- При завершении распознавания, если уже есть расставленные панели одного дизайна (подпись автозаполнения), раскладка автоматически пересчитывается под обнаруженную геометрию — пользователь видит, как плитки «встают» по стене без повторного клика
- Добавлено информационное сообщение при клике «Заполнить стену» во время активной детекции: «Определяем геометрию стены — плитки перестроятся автоматически»
- Тест: `autoFillWall` с `method:'auto'` + `wallWidthCm` в режиме перспективы успешно раскладывает плитки (раньше бросал `AutoFillBlockedError`)

---

## 08.04.2026

### Фоторедактор стен: устранение дефектов после тестирования

Проведено ручное тестирование фоторедактора. Выявлено и исправлено 10 дефектов:

- **Текстуры панелей** теперь корректно отображаются на canvas — ранее панели рисовались сплошным цветом без наложения дизайна
- **Точное размещение панелей**: клик по ячейке теперь ставит панель именно туда, куда указал пользователь, без смещения
- **Мгновенное рисование маски**: кисть и ластик рисуют в реальном времени — убрана задержка в 2–3 секунды между движением мыши и появлением мазка
- **Калибровка масштаба** снова работает корректно — восстановлена связь между кликом по canvas и установкой точек калибровки
- **Сравнение «До / После»**: «До» теперь слева, «После» справа (было перепутано)
- **Полноэкранный режим** работает в браузерах с ограничениями iframe — добавлен CSS-режим как запасной вариант
- **Убран красный круг**, который ошибочно отображался по центру canvas при редактировании маски
- **Автозаполнение стены** («Заполнить стену») снова работает — устранена проблема с инициализацией выбранного дизайна при загрузке страницы
- Добавлены информативные уведомления при автозаполнении: причина, если панели не удалось разместить

---

### Фоторедактор стен v2

Полная переработка фоторедактора стен — от загрузки фото до оформления заказа. Реализовано в 5 фазах.

**ML-распознавание стен:**
- Автоматическое определение стен и препятствий (двери, окна, мебель) на загруженном фото — распознавание работает прямо в браузере, без отправки данных на сервер
- Прогресс-бар загрузки модели при первом использовании (~15 МБ, затем кэшируется)
- При ошибке распознавания — автоматический переход на ручное выделение стены кистью

**Интерактивный canvas (react-konva):**
- Панели можно перетаскивать мышью и размещать на стене
- Рисование маски кистью/ластиком с поддержкой отмены (до 20 шагов)
- Масштабирование колёсиком (0.25–4x), перемещение canvas, pinch-to-zoom на мобильных
- Подсветка ячейки при наведении, выделение и удаление панелей кликом

**Перспектива и калибровка:**
- 4 угловых маркера для подгонки панелей под перспективу стены на фото
- Калибровка масштаба по известному объекту (дверь, розетка, окно)
- Автоматическая подстройка яркости панелей под освещение на фото

**Сохранение проектов:**
- Проекты визуализации сохраняются на сервере (фото, маска, панели, настройки перспективы)
- Автоматическое сохранение состояния в браузере при перезагрузке страницы
- API для создания, загрузки, обновления и удаления проектов

**UX-полировка:**
- Онбординг-тур из 3 шагов при первом посещении
- Сравнение «До / После» — интерактивный слайдер в модальном окне
- Скелетоны загрузки для карточек дизайнов
- Полноэкранный режим для canvas
- Информативная заглушка при отсутствии размещённых панелей
- Кнопка «Примерить на фото» в конструкторе стен

---

### Фоторедактор стен v2: исправления по код-ревью

Проведён детальный код-ревью всех 5 фаз. Выявлено и исправлено 8 критических и 14 некритических проблем:

- Устранена возможность зависания при ошибке загрузки модели распознавания — добавлены таймауты и корректная обработка сбоев
- Исправлена утечка памяти при загрузке фотографий
- Клик на canvas больше не размещает панель в неподходящих режимах
- Цены панелей вынесены в единый справочник вместо дублирования в интерфейсе
- CTA-кнопка «Заполнить стену» приведена к Design System (тёмный фон вместо зелёного)
- Каскадное удаление проектов при удалении пользователя
- Валидация имени проекта — пустое имя больше не допускается
- Оптимизация производительности сериализации маски для больших изображений

---

## 07.04.2026

### Конструктор стен: редизайн нижней части и мобильная вёрстка

- **Блок стоимости перенесён** из левой панели под визуализацию стены — логичнее в контексте рабочего процесса
- **Компактный горизонтальный layout стоимости**: статистика (панели, покрытие, стена) + цены (базовые, накладки) в CSS Grid-сетке слева, «Итого» и «В корзину» справа
- **Баннер подписки**: яркий полноширинный блок с зелёным градиентом, CTA-кнопкой, анимацией появления; для подписчиков — компактная информационная полоска
- **Размещённые панели (0 шт)**: блок легенды всегда отображается с placeholder «Добавьте панель», устраняя скачок интерфейса при добавлении первой панели
- **Кнопка «Очистить»** перенесена к «Заполнить» (логичная группировка), оставлена только иконка с tooltip
- **Свитчер размеров панели** растянут на полную ширину карточки
- **Мобильная адаптация конструктора**: CSS Grid дочерние элементы с `min-width: 0`, карточки с `overflow: hidden` и уменьшенным padding, статистика 2 колонки → 1 колонка на маленьких экранах, кнопки действий с `flex-wrap`
- **Исправлен горизонтальный скролл** на мобилке: баннер подписки ограничен `max-width: calc(100% - 24px)`, корневой контейнер с `overflowX: hidden`
- **Цена подписки обновлена**: от 7 000 ₽/мес (было 4 900 ₽/мес)

### Конструктор стен: доработки UX

- **Перетаскивание сетки**: в режиме «Интерьер» сетку можно зажать и передвинуть, чтобы точно совместить её со стеной на фото (работает и на мобиле)
- **Фото интерьера фиксировано**: при изменении размеров стены (количества ячеек) фон интерьера больше не масштабируется — меняется только сетка
- **Оттенки панелей**: цветовая тонировка стала заметно отличаться между оттенками; превью дизайна теперь тоже показывает выбранный оттенок
- **Крестик удаления**: появляется только при наведении на плитку, а не отображается постоянно
- **Исправлено ложное предупреждение**: сообщение «Невозможно разместить панель здесь» больше не появляется при перетаскивании панели на новое место

---

### Подписка: переход на тарификацию по площади

Модель подписки полностью переработана — вместо лимита на количество накладок теперь используется лимит по площади (м²/мес), как было заложено в ТЗ.

**Новые тарифы:**
- Стартовый: 15 м² — 7 000 ₽/мес
- Популярный: 30 м² — 12 000 ₽/мес
- Бизнес: безлимитная площадь — 18 000 ₽/мес

Обновлены: страница тарифов, модальное окно подписки, раздел подписки в личном кабинете, конструктор, API-эндпоинты, доменная модель, БД-миграция. Добавлена миграция localStorage для плавного перехода пользователей со старой версии.

---

### Мобильная адаптация и touch-поддержка

Весь интерфейс адаптирован под планшеты и телефоны:

- Адаптивная вёрстка на всех страницах (breakpoints: 1024px / 768px / 480px)
- Личный кабинет: боковое меню превращается в горизонтальные табы
- Фоторедактор стен: pinch-to-zoom, рисование пальцем (кисть/ластик), перемещение одним и двумя пальцами
- Плавающая кнопка поддержки на всех страницах (email, телефон, Telegram, часы работы)

---

### SEO и мета-теги

- Уникальные title и description на 17 страницах
- Open Graph и Twitter Cards для предпросмотра ссылок в соцсетях и мессенджерах
- Favicon и apple-touch-icon

---

### Docker-стек и CI/CD

- Docker Compose: nginx (reverse proxy) + frontend + backend + PostgreSQL + Redis
- Frontend собирается в multi-stage Docker-образ (Node → Nginx)
- Nginx: SPA fallback, gzip, кэш статики, security headers
- GitHub Actions: автоматический прогон линтера, typecheck, тестов и Docker build

---

### Безопасность бэкенда

- CORS: разрешён только конкретный origin фронтенда (не `*`)
- Rate limiting: login 5/min, register 3/min, forgot-password 3/min, глобально 60/min
- Защита от запуска с дефолтным JWT-секретом в production
- Security-заголовки: X-Content-Type-Options, X-Frame-Options, Referrer-Policy, Permissions-Policy

---

### Соответствие 152-ФЗ (персональные данные)

- Обязательное согласие на обработку ПД на формах регистрации, оформления заказа, обратной связи и подписки
- Страница «Политика конфиденциальности» (текст по шаблону 152-ФЗ)
- Формы не отправляются без проставленного чекбокса

---

### Фоторедактор стен: улучшения UX

- Удаление размещённой панели кликом (крестик)
- Подсветка ячейки при наведении в ручном режиме
- Сохранение проекта в браузере (автовосстановление при перезагрузке)
- Экспорт результата в JPEG
- Режим «Акцентная зона»: выделение области на стене → автоматическое заполнение панелями

---

### Контентные страницы

- **Главная**: блок «Преимущества» (6 карточек), промо-баннер «Скидка 15% на первый заказ»
- **Контакты**: интерактивная карта Яндекс.Карт с маркером офиса
- **Портфолио**: слайдер «до/после» для 3 проектов
- **Как это работает**: встроенное YouTube-видео
- **Блог**: 5 статей, отдельные страницы `/blog` и `/blog/:slug`

---

### Личный кабинет и авторизация

- Поиск в шапке сайта: иконка → разворачивающееся поле → переход в каталог с результатами
- Избранное в шапке с бейджем количества
- Восстановление пароля: ввод email → код (6 цифр, действует 15 мин) → новый пароль
- Кнопки «Войти через Google» и «Войти через VK» (placeholder, скоро)
- Раздел «Уведомления» в ЛК: переключатели email-рассылок
- Кнопка «Повторить заказ» — копирует товары из прошлого заказа в корзину

---

### Оформление заказа: дата монтажа, СБП, быстрая примерка

- Выбор даты и времени монтажа при оформлении (только будущие даты, рабочие часы 9:00–20:00)
- Оплата через СБП (Система Быстрых Платежей) — 4-й способ оплаты
- Кнопки «Примерить на фото» и «Примерить в конструкторе» на страницах каталога и товара
- Переход из карточки товара сразу в конструктор или фоторедактор с предвыбранным дизайном

---

### Дизайн-система: приведение к брендбуку

Визуальная часть приведена в соответствие с утверждённым DESIGN-SYSTEM.md:

- Акцентный цвет: зелёный `#4CAF50` (был синий)
- CTA-кнопки: тёмный фон, белый текст
- Единые скругления: кнопки 8px, карточки 16px, инпуты 8px, бейджи 6px
- Цветовая палитра текста и бордеров по брендбуку

---

### Отзывы и фильтры каталога

- **Отзывы на странице товара**: список с рейтингом-звёздами, пагинация, форма (только для авторизованных)
- **Фильтры каталога**: по цвету (цветные кружки), по стилю (выпадающий список), toggle «Новинки»
- Фильтры работают в любых комбинациях и совместимы с поиском

---

### Подключение PostgreSQL

Переход с хранения данных в памяти на полноценную базу данных:

- Схема БД: 9 таблиц с внешними ключами и каскадным удалением
- 7 репозиториев для работы с БД через SQLAlchemy (async)
- Скрипт заполнения начальными данными (12 дизайнов, 6 категорий)
- Режим in-memory сохранён как fallback для тестов и разработки без БД

---

## До 07.04.2026

### MVP — запуск продукта

Создание Wonder Wow Wall с нуля: B2C интернет-магазин магнитных стеновых панелей.

- **Каталог**: карточки дизайнов с фото, рейтингом, ценами, бейджами
- **Страница товара**: галерея, описание, характеристики, кнопка «В корзину»
- **Конструктор стен**: grid-система с привязкой к сетке, выбор размеров панелей и дизайнов, калькулятор стоимости
- **Подписочная модель**: 3 тарифа, модальное окно оформления, скидка для подписчиков
- **Корзина и оформление заказа**: многошаговый checkout, выбор доставки и оплаты
- **Авторизация**: регистрация, вход, JWT-токены
- **Личный кабинет**: профиль, заказы, избранное, адреса
- **Фоторедактор стен**: загрузка фото интерьера, выделение стены (кисть/ластик), размещение панелей, автозаполнение, калькулятор стоимости
- **Backend API**: FastAPI, DDD-архитектура (домены, use cases, репозитории)
- **Интеграция**: фронтенд подключён к бэкенду через API-хуки (React Query)

---

## Стек технологий

| Компонент | Технологии |
|-----------|-----------|
| Frontend | React, TypeScript, Zustand, Ant Design, Vite, react-konva, @huggingface/transformers |
| Backend | Python, FastAPI, SQLAlchemy (async), Alembic |
| Хранение | PostgreSQL, Redis |
| Тесты | pytest (backend), Vitest (frontend) |
| Деплой | Docker Compose (Nginx + Frontend + Backend + DB + Redis) |
| CI/CD | GitHub Actions |
