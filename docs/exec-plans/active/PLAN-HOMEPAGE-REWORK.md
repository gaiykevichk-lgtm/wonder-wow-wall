# PLAN: Переработка главной страницы (HomePage)

**Статус:** Pending  
**Дата создания:** 2026-05-11  
**Цель:** Привести главную страницу в соответствие с обновлённым видением платформы: два режима каталога (коллекции / текстуры), конфигуратор с ИИ-дизайнером и готовыми решениями, быстрый расчёт с упаковками, информация о шоу-румах

---

## Обзор

Заказчик определил платформу Wonder Wow Wall как комплексное цифровое решение: демонстрация продукции, ИИ-дизайнер, автоматический расчёт материалов, оформление заказов, подписка и информирование о шоу-румах/дилерах. Текущая главная страница (`HomePage.tsx`, 1665 строк, 9 секций) ориентирована на простой каталог панелей и не отражает новый функционал.

### Что меняется

| Текущая секция | Действие | Новая секция |
|---|---|---|
| HeroSection | ПЕРЕРАБОТКА | Новое позиционирование: дизайн-решения, ИИ, шоу-румы |
| HowItWorksSection | ПЕРЕРАБОТКА | Обновлённый путь пользователя: Каталог → Конфигуратор → Расчёт → Заказ |
| AdvantagesSection | ПЕРЕРАБОТКА | Обновлённые преимущества: ИИ-дизайнер, AR, подписка, шоу-румы |
| PromoBannerSection | ПЕРЕРАБОТКА | Подписка и расчёт вместо промокода |
| CategoriesSection | ЗАМЕНА | Два режима каталога: «По коллекциям» / «По текстурам» |
| PopularProductsSection | ПЕРЕРАБОТКА | Популярные интерьерные решения вместо отдельных продуктов |
| CalculatorCTASection | ПЕРЕРАБОТКА | Быстрый расчёт: площадь → упаковки → стоимость |
| ReviewsSection | ОБНОВЛЕНИЕ | Расширенные отзывы с фото интерьеров |
| CTABannerSection | ПЕРЕРАБОТКА | Обновлённые CTA: Каталог / Конфигуратор / Связаться |
| *(новая)* | ДОБАВЛЕНИЕ | Секция «Конфигуратор»: ИИ-дизайнер + Готовые решения |
| *(новая)* | ДОБАВЛЕНИЕ | Секция «Шоу-румы и дилеры» |

### Зависимости от других планов

- **PLAN-PANEL-CONFIGURATOR** (завершён, в completed/) — главная использует API текстур и коллекций. Фазы 1–6 этого плана частично реализованы, но часть endpoints, необходимых для HomePage, отсутствуют (см. секцию «Риски реализации»).
- Каталог (страница `/catalog`) и Конфигуратор (страница `/configurator`) — отдельные планы. Каталог в текущем состоянии фильтрует по размеру (30×30 / 30×60 / 60×60), а не по коллекциям/текстурам. Необходимо убедиться, что маршруты `/catalog?mode=collections` и `/catalog?mode=textures` поддерживаются до начала Фазы 2.

---

## Риски реализации

### Риск 1: Несуществующие API endpoints

**Фазы:** 2, 3, 5, 6, 7

**Суть:** План описывает endpoints, которых нет в текущей кодовой базе:

| Endpoint | Фаза | Статус |
|---|---|---|
| `GET /api/collections` | 2, 5 | **Отсутствует** — сущность Collection не существует |
| `GET /api/textures` → `design_count` | 2, 5 | **Отсутствует** — в ответе нет `design_count` |
| `GET /api/solution-categories` | 3 | **Отсутствует** — нужно создать справочник |
| `GET /api/featured-solutions` | 5 | **Отсутствует** — интерьерные решения не реализованы |
| `GET /api/showrooms` | 7 | **Отсутствует** — домен `content/` не существует |
| `Showroom` entity | 7 | **Отсутствует** |
| `PackagingSpec` (panels_per_package, coverage_per_package) | 6 | **Отсутствует** — есть только `PanelSize` с ценой и габаритами |
| `POST /api/calculate` | 6 | **Отсутствует** — существующий `POST /api/calculator` (contacts) не использует packaging logic |
| `CompanionPanel` / panel compatibility | 5 | **Отсутствует** — логика совместимости панелей не реализована |

**Митигация:**
- Перед началом Фазы 2 — провести аудит реализованных endpoints
- Для отсутствующих endpoints — добавить реализацию в каждую фазу как подзадачу
- В Фазе 2 сначала реализовать `GET /api/collections` и `design_count` для textures, затем frontend
- В Фазе 7 сначала создать `domain/content/` bounded context (или решить, что Showroom — часть существующего домена, например `domain/analytics/`)

---

### Риск 2: Пересечение с существующей логикой — каталог фильтруется по размеру

**Фазы:** 2, 5

**Суть:** Текущий `CatalogPage.tsx` использует фильтрацию по `sizeTab` (ключи `cat-30x30`, `cat-30x60`, `cat-60x60`). План вводит два режима: `mode=collections` и `mode=textures` — это полностью меняет парадигму навигации в каталоге.

**Митигация:**
- Не начинать frontend Фазы 2, пока не подтверждено, что `/catalog?mode=collections` и `/catalog?mode=textures` корректно обрабатываются на бэкенде
- Добавить в план задачу: обновить `CatalogPage.tsx` для поддержки режимов (`mode` param), иначе ссылки с HomePage ведут в никуда
- Лучший подход — сделать `mode` параметром, который переключает layout каталога: размерная сетка (текущее) vs коллекции vs текстуры

---

### Риск 3: Фаза 6 — расчёт с упаковками требует данных, которых нет

**Фазы:** 6

**Суть:** Для расчёта `package_count = ceil(wall_area / coverage_per_package)` нужны:
- `coverage_per_package` (м² на упаковку) — **не существует** ни в какой форме
- `panels_per_package` — **не существует**

При этом в `domain/catalog/value_objects.py` уже есть `BASE_PANEL_PRICES` с ценами за панель, но цена за упаковку не определена.

**Митигация:**
- Добавить задачу в Фазу 6.1: определить packaging specs (возможно, hardcoded константой в code или справочником в БД)
- Перед реализацией `POST /api/calculate` — уточнить у заказчика, известны ли площади покрытия и количество панелей в упаковке
- Если данных нет — добавить в план отдельный этап: сбор/уточнение packaging specs у product owner

---

### Риск 4: Фаза 7 — создание нового bounded context `content/`

**Фазы:** 7

**Суть:** План предлагает создать `domain/content/` для `Showroom` entity. Однако доменная модель проекта уже содержит 8 bounded contexts. Добавление девятиого для одной сущности может быть overhead'ом.

**Митигация:**
- Рассмотреть альтернативы: `Showroom` как часть существующего домена (Analytics? Shop? или новый небольшой `Contact` bounded context)
- Или — сделать `Showroom` частью `domain/analytics/` (отделение location data от бизнес-логики)
- Решение принять до начала Фазы 7

---

### Риск 5: Потенциальные регрессии при обновлении HeroSection и HowItWorksSection

**Фазы:** 1, 4

**Суть:** Эти секции обновляются на месте (in-place edit). При изменении пропсов и анимаций легко сломать:
- Responsive breakpoints (768px) — если изменить структуру без обновления media queries
- Навигационные обработчики (`navigate('/catalog')`, `navigate('/configurator')`) — если ошибочно удалить или изменить callback
- Фирменные цвета — если добавить inline color без использования констант

**Митигация:**
- Фаза 1 и Фаза 4 должны включать визуальную проверку (screenshot) до и после изменений
- Использовать существующие `fadeUpVariants` и `containerVariants` без изменений сигнатуры
- Все color values — только из констант файла

---

### Риск 6: Фаза 5 — изменение ProductPage при смене текстур

**Фазы:** 5

**Суть:** Фаза 5 описывает «при смене текстуры — изображение интерьера обновляется». Это подразумевает, что компонент смены текстуры должен обращаться к `GET /api/designs/{design_id}/variant-image?texture_id=&color_id=` (уже существует) или к `GET /api/featured-solutions` (не существует).

**Митигация:**
- `variant-image` endpoint уже реализован — использовать его
- `featured-solutions` (комплексный endpoint с interior_images и companion_panels) — требует отдельной реализации
- Клиентская смена текстуры на главной странице — это preview, не полная загрузка всех variant images
- Рассмотреть: загрузка только thumbnail'ов для preview, полное изображение — по клику на карточку

---

### Риск 7: Edge cases — пустые состояния

**Фазы:** 2, 3, 5, 7

**Суть:** План не покрывает случаи:
- Нет коллекций (0 results от `GET /api/collections`) → секция не должна показывать пустую сетку
- Нет текстур с design_count > 0 → категории текстур не показываем
- Нет шоу-румов (`GET /api/showrooms` → []) → секция скрывается (уже есть в критериях, хорошо)
- Нет интерьерных изображений для `featured-solutions` → placeholder или пропуск карточки?
- Все 4 панели-компаньона недоступны → пустой массив показывать или скрывать блок?

**Митигация:**
- Во всех секциях добавить: `data?.length === 0 ? null : <Section />` для пустых состояний
- Для `featured-solutions` — fallback на `PopularProductsSection` (старая версия) если API не вернул данных
- Добавить в каждую фазу explicit задачу: обработка edge case пустого state

---

### Риск 8: Фаза 9 — финальная интеграция 11 секций в одном файле

**Фазы:** 9

**Суть:** `HomePage.tsx` после всех изменений может превысить 2500 строк. Декомпозиция в отдельные файлы (рекомендованная в «Общих замечаниях») не сделана — это создаёт риск:
- Merge conflicts при одновременной работе над файлом
- Сложность навигации — все секции в одном файле сложно искать
- Тестирование — нельзя тестировать секции изолированно

**Митигация:**
- До Фазы 9 выполнить декомпозицию (сделать это подготовительным шагом перед Фазой 1)
- Добавить в план задачу: перед началом Фазы 1 — выделить каждую секцию в отдельный файл в `content/ui/home/`
- Или — принять решение работать в одном файле, но тогда критически важно вести changelog изменений внутри файла (комментарии-ориентиры)

---

### Риск 9: Snapshot-тесты хрупкие и тяжёлые для sandbox

**Фазы:** 4, 8, 9

**Суть:** План упоминает snapshot-тесты для `HowItWorksSection`, `PromoBannerSection`, `AdvantagesSection`. Snapshot-тесты:
- Хрупкие — любое незначительное изменение (отступ, цвет) ломает тест
- Тяжёлые — требуют рендера всей секции, что в sandbox с ограниченным RAM может вызывать OOM
- Сложно поддерживать — обновление snapshot требует ручного подтверждения

**Митигация:**
- Убрать snapshot-тесты из плана, заменить на структурные assertions (проверка наличия элементов, но не точному match'у)
- Для Фаз 4, 8, 9: использовать unit-тесты с `screen.getByText`, `screen.getByRole` вместо snapshot
- Если snapshot всё же нужен — запускать только для конкретной секции, не для всего HomePage

---

### Риск 10: Неявная зависимость — `/configurator` route не существует

**Фазы:** 1, 2, 3, 9

**Суть:** План ссылается на маршрут `/configurator` в нескольких местах:
- HeroSection CTA: `navigate('/configurator')` → Фаза 1
- CatalogModesSection CTA → Фаза 2
- ConfiguratorShowcaseSection CTA → Фаза 3
- CTABannerSection CTA → Фаза 9

Но в текущем router (`shared/router.tsx`) route `/configurator` не зарегистрирован. Существуют:
- `/constructor` (wall editor)
- `/visualizer` (photo editor)
- `/catalog`

**Митигация:**
- Добавить задачу в план: проверить существование `/configurator` route; если его нет — создать страницу `ConfiguratorPage` или использовать существующий `/constructor` с параметром `?mode=individual`
- Согласовать с заказчиком: что именно понимается под `/configurator` — это новая страница или адаптер над существующим `/constructor`?

---

## Что меняется

### Порядок секций на обновлённой главной

```
1. HeroSection              — Первый экран с value proposition
2. CatalogModesSection      — Два режима каталога (NEW)
3. ConfiguratorShowcase      — ИИ-дизайнер + Готовые решения (NEW)
4. HowItWorksSection        — 4 шага пользовательского пути
5. PopularSolutionsSection  — Популярные интерьерные решения
6. CalculatorSection        — Быстрый расчёт с упаковками
7. AdvantagesSection        — Преимущества платформы
8. ShowroomsSection         — Шоу-румы и дилерские центры (NEW)
9. PromoBannerSection       — Подписка и условия
10. ReviewsSection          — Отзывы клиентов
11. CTABannerSection        — Финальный призыв к действию
```

---

## Дизайн-система

### Фирменные цвета

Мы сохраняем существующую фирменную палитру Wonder Wow Wall. Все секции **обязаны** использовать только эти цвета — никаких отступлений на усмотрение разработчика:

| Константа | Значение | Использование |
|---|---|---|
| `ACCENT` | `#4CAF50` | Основной зелёный, CTA, акценты |
| `ACCENT_DARK` | `#2E7D32` | Hover-состояния, градиенты |
| `DARK` | `#2D2D2D` | Основной текст |
| `GRAY_TEXT` | `#6B7280` | Вторичный текст, описания |
| `LIGHT_BG` | `#F5F5F5` | Фоновые секции |
| `SUBTLE_BORDER` | `rgba(0,0,0,0.04)` | Разделители, карточки |

Эти константы уже определены в текущем `HomePage.tsx`. При добавлении новых секций — использовать их, **не добавлять новые цвета**.

### UI/UX Pro Max

При разработке каждой секции — применять рекомендации из скилла `ui-ux-pro-max`:

```bash
# Перед началом работы над секцией — поиск релевантных стилей
python3 ~/.claude/skills/ui-ux-pro-max/scripts/search.py "<ключевые слова>" --domain <домен>
```

**Порядок применения для каждой секции:**

1. **Style** — поиск стилей, релевантных продукту (например, "e-commerce landing minimal")
2. **Color** — уточнить палитру для нашего product type (saas, e-commerce)
3. **Typography** — подобрать font pairing (если меняем шрифты)
4. **UX** — проверить accessibility, animation, spacing
5. **Stack (react)** — применить React-specific гайдлайны

**Домены для поиска:**

| Домен | Когда использовать |
|---|---|
| `style` | Glassmorphism, minimalism, bento grid и т.д. |
| `color` | Палитры для e-commerce, service |
| `typography` | Font pairings для landing pages |
| `landing` | Структура страниц, CTA-стратегии |
| `ux` | Accessibility, animation, spacing rules |
| `react` | React-специфичные паттерны |

### Responsive breakpoints

Использовать только `768px` breakpoint (как в текущем коде). Дополнительные breakpoint'ы не добавлять.

### Sandbox-ограничения (ТЕСТЫ)

**⚠️ ВНИМАНИЕ: Слабый sandbox**

Наш sandbox имеет ограниченные ресурсы. При запуске тестов **обязательно** соблюдать следующие правила:

- **Запускать не более 1 теста за раз** или батчами по 2–5 тестов
- **Не запускать тяжёлые тесты** (с множеством renders, snapshot-тесты) параллельно
- Если sandbox крашится (`kill` или OOM) — уменьшить batch до 1 и добавить `sleep 5` между запусками
- Для frontend: `NODE_OPTIONS='--max-old-space-size=512'` при запуске через vitest
- Для backend: запускать pytest с `--timeout=30` и `-x` (остановиться на первом failure)
- Все long-running commands (>2 min) запускать через `run_in_background: true`

**Команды запуска тестов:**

```bash
# Frontend (vitest) — по 1 тесту
NODE_OPTIONS='--max-old-space-size=512' npx vitest run src/domains/content/ui/__tests__/HomePage.test.tsx --reporter=verbose

# Backend (pytest) — по 1 тесту, с остановкой на первом failure
cd backend && python -m pytest tests/ -x -v --timeout=30
```

---

## Фаза 1: Hero Section — Новое позиционирование

**Цель:** Обновить первый экран для отражения полного спектра возможностей платформы.

### 1.1 Frontend

**Файл:** `frontend/src/domains/content/ui/HomePage.tsx` → секция `HeroSection`

**Изменения:**

- [ ] Обновить заголовок hero: акцент на «дизайн-решения для стен», а не просто «3D-панели»
- [ ] Обновить подзаголовок: упомянуть ИИ-дизайнер, каталог коллекций, автоматический расчёт
- [ ] Заменить CTA-кнопки:
  - Основная: «Подобрать решение» → `/configurator` (конфигуратор)
  - Вторичная: «Смотреть каталог» → `/catalog`
- [ ] Обновить trust badges: заменить «200+ дизайнов» на актуальные метрики (кол-во текстур, коллекций, шоу-румов)
- [ ] Заменить hero-изображения: вместо Unsplash — реальные интерьеры из ассетов проекта или placeholder'ы с правильной семантикой
- [ ] Обновить тег над заголовком: вместо «Сделано в России — 2026» → «Дизайн-платформа для стен»
- [ ] Обновить PageMeta: title и description для SEO

**Стилевые правила (CONVENTIONS.md):**
- Inline styles only (React.CSSProperties)
- Framer Motion для анимаций (fadeUpVariants, containerVariants)
- Цветовые константы из верха файла (ACCENT, DARK, GRAY_TEXT)
- Ant Design Button для CTA

### 1.2 Backend

Нет backend-изменений для данной фазы. Hero-контент статический.

### 1.3 Тесты

- [ ] Unit-тест: `HeroSection` рендерится без ошибок
- [ ] Unit-тест: CTA-кнопки вызывают navigate с правильными путями (`/configurator`, `/catalog`)
- [ ] Unit-тест: PageMeta содержит обновлённые title и description
- [ ] Визуальная проверка: скриншот hero на desktop (1200px) и mobile (375px)

**Файл тестов:** `frontend/src/domains/content/ui/__tests__/HomePage.test.tsx`

### Критерии приёмки

- Hero отражает позиционирование «дизайн-платформа», а не «магазин панелей»
- CTA ведут на `/configurator` и `/catalog`
- Responsive layout работает на mobile (768px breakpoint)
- Нет hardcoded Unsplash URLs для production-ассетов

---

## Фаза 2: Секция «Каталог» — Два режима просмотра

**Цель:** Заменить текущую `CategoriesSection` (категории по размерам) на секцию с двумя входными точками: «По коллекциям» и «По текстурам».

**Зависимости:** API текстур из PLAN-PANEL-CONFIGURATOR (Фаза 1, уже реализовано — `GET /api/textures`).

### 2.1 Backend

**Файлы:**
- `backend/app/domain/catalog/entities.py` — проверить наличие поля `collection` / добавить
- `backend/app/application/catalog/dtos.py` — DTO для списка коллекций
- `backend/app/infrastructure/api/catalog.py` — новый endpoint

**Изменения:**

- [ ] **Модель коллекции:** Проверить, есть ли в Design поле `style` / `collection`. Если нет — добавить Value Object `Collection` (name, slug, description, cover_image) в `domain/catalog/`
- [ ] **API endpoint** `GET /api/collections` — список коллекций с обложками и счётчиком дизайнов
  - Response: `[{ id, name, slug, description, cover_image, design_count }]`
  - Сортировка по `sort_order`
- [ ] **API endpoint** `GET /api/textures/summary` — краткий список текстур для главной (slug, name, swatch_image, design_count)
  - Или расширить существующий `GET /api/textures` полем `design_count`
- [ ] Миграция Alembic, если добавляются новые таблицы/поля

**Conventions (backend):**
- Domain entity — `@dataclass` в `domain/catalog/`
- Repository interface — ABC в `domain/catalog/repositories.py`
- Use case — класс с `execute()` в `application/catalog/use_cases.py`
- Router — в `infrastructure/api/catalog.py`
- DTO — Pydantic schema в `application/catalog/dtos.py`

### 2.2 Frontend

**Файл:** `frontend/src/domains/content/ui/HomePage.tsx` → новая секция `CatalogModesSection`

**Изменения:**

- [ ] Удалить текущую `CategoriesSection` (фильтрация по размерам 30×30, 30×60, 60×60)
- [ ] Создать `CatalogModesSection` с двумя крупными карточками:
  - **«По коллекциям»** — карточка с preview коллекции (панели в белом цвете), CTA → `/catalog?mode=collections`
  - **«По текстурам»** — карточка с preview текстур (swatch-сетка), CTA → `/catalog?mode=textures`
- [ ] Каждая карточка: заголовок, краткое описание, превью-изображение, hover-эффект
- [ ] Под карточками: список текстур-категорий в виде горизонтального scroll (дерево, бетон, мрамор, ткань, кожа, покраска) — каждый элемент ведёт на `/catalog?mode=textures&texture={slug}`
- [ ] Данные: загрузка через TanStack Query (`useQuery`) — `GET /api/collections`, `GET /api/textures`
- [ ] Fallback: пока API грузится — skeleton из Ant Design

**Conventions (frontend):**
- Inline styles, Framer Motion анимации
- TanStack Query для серверных данных
- `useNavigate()` для навигации

### 2.3 Тесты

**Backend:**
- [ ] Pytest: `GET /api/collections` возвращает список с обложками и счётчиками
- [ ] Pytest: `GET /api/textures` содержит `design_count` для каждой текстуры
- [ ] Pytest: пустой результат, если нет активных коллекций/текстур

**Frontend:**
- [ ] Unit-тест: `CatalogModesSection` рендерит две карточки режимов
- [ ] Unit-тест: клик по карточке «По коллекциям» → navigate `/catalog?mode=collections`
- [ ] Unit-тест: клик по текстуре → navigate `/catalog?mode=textures&texture={slug}`
- [ ] Unit-тест: skeleton отображается при loading state

### Критерии приёмки

- На главной видны две точки входа в каталог (коллекции и текстуры)
- Текстурные категории соответствуют документу: дерево, бетон, мрамор, ткань, кожа, покраска
- Данные загружаются с backend, нет hardcoded mock'ов для production
- Responsive: на mobile карточки режимов — stack, текстуры — horizontal scroll

---

## Фаза 3: Секция «Конфигуратор» — ИИ-дизайнер и готовые решения

**Цель:** Добавить новую секцию, демонстрирующую два режима конфигуратора: «Индивидуальный подбор» (ИИ-дизайнер) и «Готовые решения».

### 3.1 Backend

**Файлы:**
- `backend/app/domain/catalog/` — Value Object для категорий готовых решений
- `backend/app/infrastructure/api/catalog.py` — endpoint для категорий решений

**Изменения:**

- [ ] **Модель категорий готовых решений:** Создать enum / справочник категорий:
  ```
  Для дома:
    Гостиная (зона ТВ / зона дивана / все стены)
    Спальня (зона изголовья / зона ТВ / все стены)
    Детская (зона у кроватки / все стены)
    Кухня
    WC
  Для бизнеса:
    Офис, Отель, Ресторан, Кофейня, Ритейл
  Социальные объекты:
    Школы, Больницы, Детские сады, Муниципальные, Транспортные
  ```
- [ ] **API endpoint** `GET /api/solution-categories` — дерево категорий готовых решений для конфигуратора
  - Response: `[{ key, label, icon, children: [{ key, label, zones?: string[] }] }]`
- [ ] Данные могут быть статическими (конфиг-файл), не обязательно в БД, но endpoint нужен для единой точки правды

**Conventions (backend):**
- Если данные статические — можно вынести в `domain/catalog/solution_categories.py` как константу
- Endpoint всё равно нужен — фронтенд не должен дублировать справочники

### 3.2 Frontend

**Файл:** `frontend/src/domains/content/ui/HomePage.tsx` → новая секция `ConfiguratorShowcaseSection`

**Изменения:**

- [ ] Создать `ConfiguratorShowcaseSection` с двумя блоками:
  - **«Индивидуальный подбор»** (левый блок):
    - Иконка/иллюстрация ИИ-дизайнера
    - Заголовок: «ИИ подберёт дизайн под ваш интерьер»
    - Краткое описание: загрузите фото → получите варианты → скорректируйте
    - CTA: «Попробовать» → `/configurator?mode=individual`
    - Визуальный акцент (gradient overlay, AI-иконка)
  - **«Готовые решения»** (правый блок):
    - Превью категорий (Для дома / Для бизнеса / Социальные объекты) — 3 карточки-иконки
    - Заголовок: «Вдохновляйтесь готовыми интерьерами»
    - CTA: «Смотреть решения» → `/configurator?mode=solutions`
- [ ] Данные категорий: загрузка через `useQuery` → `GET /api/solution-categories`
- [ ] Layout: 2 колонки на desktop, stack на mobile
- [ ] Hover-эффекты на карточках режимов (lift + shadow, как в существующих секциях)

### 3.3 Тесты

**Backend:**
- [ ] Pytest: `GET /api/solution-categories` возвращает 3 корневых категории (дом, бизнес, социальные)
- [ ] Pytest: каждая корневая категория содержит children

**Frontend:**
- [ ] Unit-тест: `ConfiguratorShowcaseSection` рендерит оба блока
- [ ] Unit-тест: CTA «Попробовать» → navigate `/configurator?mode=individual`
- [ ] Unit-тест: CTA «Смотреть решения» → navigate `/configurator?mode=solutions`
- [ ] Unit-тест: загрузка категорий и отображение трёх корневых групп

### Критерии приёмки

- На главной два явных входа в конфигуратор
- ИИ-дизайнер визуально выделен как ключевая фича (не просто текст)
- Категории готовых решений соответствуют документу (дом/бизнес/соц. объекты)
- Responsive: колонки становятся stack на mobile

---

## Фаза 4: Секция «Как это работает» — Обновлённый пользовательский путь

**Цель:** Обновить `HowItWorksSection` для отражения нового пользовательского пути: Каталог/Конфигуратор → Примерка → Расчёт → Заказ.

### 4.1 Backend

Нет backend-изменений. Контент статический.

### 4.2 Frontend

**Файл:** `frontend/src/domains/content/ui/HomePage.tsx` → секция `HowItWorksSection`

**Изменения:**

- [ ] Обновить шаги (`steps` массив) в соответствии с документом:
  1. **«Выберите стиль»** — «Просматривайте каталог по коллекциям или текстурам. Нашли понравившуюся панель — примерьте разные текстуры прямо в карточке товара.»
  2. **«Примерьте на стену»** — «Загрузите фото комнаты — ИИ-дизайнер предложит варианты оформления. Или выберите из готовых интерьерных решений.»
  3. **«Рассчитайте стоимость»** — «Укажите размеры стены. Система автоматически рассчитает количество упаковок и итоговую стоимость.»
  4. **«Оформите заказ»** — «Выберите покупку или подписку. Подтвердите адрес, способ установки — и готово.»
- [ ] Обновить SVG-иконки шагов для соответствия новым описаниям
- [ ] Сохранить существующую анимационную механику (fadeUpVariants, hover-эффекты, StepIcon)

### 4.3 Тесты

- [ ] Unit-тест: рендерятся 4 карточки шагов с обновлёнными заголовками
- [ ] Unit-тест: SVG-иконки отображаются для каждого шага
- [ ] Snapshot-тест: обновить snapshot для `HowItWorksSection`

### Критерии приёмки

- 4 шага отражают новый путь: выбор стиля → примерка (ИИ/готовые) → расчёт → заказ
- Упоминание ИИ-дизайнера и подписки в описаниях шагов
- Визуальный стиль не ломается (шрифты, отступы, анимации)

---

## Фаза 5: Секция «Популярные решения» — Интерьерные решения

**Цель:** Заменить текущую `PopularProductsSection` (4 карточки продуктов) на секцию с популярными интерьерными решениями, включая смену текстур и панели-компаньоны.

**Зависимости:** API текстур и variant images (PLAN-PANEL-CONFIGURATOR, уже реализовано).

### 5.1 Backend

**Файлы:**
- `backend/app/application/catalog/use_cases.py` — use case для featured-решений
- `backend/app/infrastructure/api/catalog.py` — endpoint

**Изменения:**

- [ ] **API endpoint** `GET /api/featured-solutions` — подборка популярных интерьерных решений для главной
  - Response: `[{ id, title, room_type, design: { id, name, form_image }, texture: { id, name, swatch }, interior_images: string[], companion_panels: [{ id, name, image }] }]`
  - Limit: 4–6 решений
  - Сортировка: по popularity / ручная курация (admin)
- [ ] **Use case** `ListFeaturedSolutions` — собирает данные из designs, textures, variant_images + interior examples
- [ ] Если интерьерные изображения ещё не в системе — добавить поле `interior_images: list[str]` к VariantImage или отдельную сущность `InteriorExample`

### 5.2 Frontend

**Файл:** `frontend/src/domains/content/ui/HomePage.tsx` → секция `PopularSolutionsSection` (заменяет `PopularProductsSection`)

**Изменения:**

- [ ] Заменить `PopularProductsSection` на `PopularSolutionsSection`
- [ ] Карточка решения:
  - Изображение интерьера (основное)
  - Название решения и тип комнаты (бейдж)
  - Текстура-свотч + название текстуры
  - Кнопка «Сменить текстуру» — раскрывает горизонтальный row свотчей
  - При смене текстуры — изображение интерьера обновляется (если есть variant)
  - Блок «Панели-компаньоны» — миниатюры 2–3 совместимых панелей
  - CTA: клик по карточке → `/product/{design_id}?texture={texture_slug}`
- [ ] Header секции: «Популярные интерьерные решения» + ссылка «Все решения →» → `/configurator?mode=solutions`
- [ ] Данные: `useQuery` → `GET /api/featured-solutions`
- [ ] Skeleton loading state

### 5.3 Тесты

**Backend:**
- [ ] Pytest: `GET /api/featured-solutions` возвращает массив с интерьерными решениями
- [ ] Pytest: каждое решение содержит design, texture, interior_images
- [ ] Pytest: companion_panels присутствуют (массив, может быть пуст)

**Frontend:**
- [ ] Unit-тест: `PopularSolutionsSection` рендерит карточки решений
- [ ] Unit-тест: клик по свотчу текстуры обновляет state
- [ ] Unit-тест: клик по карточке → navigate с правильными параметрами
- [ ] Unit-тест: skeleton при loading

### Критерии приёмки

- На главной — интерьерные решения вместо «голых» продуктов
- Видна текстура каждого решения + возможность смены (превью)
- Панели-компаньоны отображаются
- Данные грузятся с API, не hardcoded

---

## Фаза 6: Секция «Калькулятор» — Быстрый расчёт с упаковками

**Цель:** Обновить `CalculatorCTASection` в соответствии с новой логикой расчёта: площадь стены → типоразмер панелей → количество упаковок (с округлением вверх) → стоимость → выбор «Подписка» / «Купить».

### 6.1 Backend

**Файлы:**
- `backend/app/domain/catalog/` — Value Object `PackagingSpec`
- `backend/app/domain/order/services.py` — обновить `PricingService`
- `backend/app/application/order/use_cases.py` — use case `CalculateOrder`
- `backend/app/infrastructure/api/` — endpoint расчёта

**Изменения:**

- [ ] **Value Object** `PackagingSpec`:
  ```python
  @dataclass(frozen=True)
  class PackagingSpec:
      panel_size: str           # "300x300", "300x600", "600x600"
      panels_per_package: int   # Кол-во панелей в упаковке
      coverage_per_package: float  # Площадь покрытия одной упаковки (м²)
  ```
- [ ] **Хранение** `packaging_specs` — конфиг или таблица в БД (для каждого типоразмера)
- [ ] **API endpoint** `POST /api/calculate` — быстрый расчёт:
  - Request: `{ wall_area: float, panel_size: string, design_mode: "mono" | "combined", panels?: [{ size, percentage }] }`
  - Response: `{ packages: [{ panel_size, package_count, panels_total, coverage }], total_price: int, subscription_monthly: int }`
  - Логика: `package_count = ceil(wall_area / coverage_per_package)`
- [ ] **Use case** `CalculateQuickEstimate` — валидация входных данных, расчёт, формирование ответа

### 6.2 Frontend

**Файл:** `frontend/src/domains/content/ui/HomePage.tsx` → секция `CalculatorSection` (заменяет `CalculatorCTASection`)

**Изменения:**

- [ ] Заменить статические «Примеры цен» на интерактивный мини-калькулятор:
  - Поле ввода площади стены (м²) — `InputNumber` из Ant Design
  - Выбор типоразмера панели — `Radio.Group` (30×30 / 30×60 / 60×60)
  - Toggle «Моно / Комбинированный» — `Switch` из Ant Design
  - Результат: количество упаковок, площадь покрытия, итоговая стоимость
- [ ] Две кнопки результата:
  - «Оформить подписку — {monthly_price} ₽/мес» + иконка `?` с Tooltip (условия подписки)
  - «Купить — {total_price} ₽»
- [ ] При изменении параметров — debounced запрос к `POST /api/calculate`
- [ ] Fallback: пока нет API-ответа — показывать skeleton или заглушку «Укажите параметры»
- [ ] Левая часть: заголовок + описание + trust items (обновлённые)
- [ ] Правая часть: интерактивный калькулятор

### 6.3 Тесты

**Backend:**
- [ ] Pytest: `POST /api/calculate` с площадью 10 м² и панелями 30×30 → корректное кол-во упаковок
- [ ] Pytest: округление вверх до целой упаковки (ceil)
- [ ] Pytest: комбинированный режим (несколько типоразмеров с процентами)
- [ ] Pytest: валидация — отрицательная площадь, неизвестный типоразмер → 400

**Frontend:**
- [ ] Unit-тест: мини-калькулятор рендерится с полями ввода
- [ ] Unit-тест: изменение площади триггерит запрос (debounce)
- [ ] Unit-тест: результат отображает кол-во упаковок и стоимость
- [ ] Unit-тест: Tooltip подписки отображается при hover на `?`
- [ ] Unit-тест: кнопки «Подписка» и «Купить» ведут на `/checkout`

### Критерии приёмки

- Расчёт кратен целой упаковке (округление вверх)
- Для каждого типоразмера задано кол-во панелей в упаковке и площадь покрытия
- Результат содержит и стоимость покупки, и стоимость подписки
- Tooltip объясняет условия подписки
- Responsive: на mobile калькулятор — stack layout

---

## Фаза 7: Секция «Шоу-румы» — Офлайн-точки (НОВАЯ)

**Цель:** Добавить новую секцию с информацией о шоу-румах и дилерских центрах для офлайн-знакомства с продуктом.

### 7.1 Backend

**Файлы:**
- `backend/app/domain/content/` — новый bounded context или расширение существующего
- `backend/app/domain/content/entities.py` — entity `Showroom`
- `backend/app/domain/content/repositories.py` — `ShowroomRepository`
- `backend/app/infrastructure/persistence/models.py` — ORM-модель
- `backend/app/infrastructure/api/content.py` — API router

**Изменения:**

- [ ] **Entity** `Showroom`:
  ```python
  @dataclass
  class Showroom:
      id: str
      name: str              # "Шоу-рум Москва Центр"
      city: str              # "Москва"
      address: str           # "ул. Примерная, 1"
      phone: str
      working_hours: str     # "Пн-Пт 10:00–20:00, Сб 10:00–18:00"
      latitude: float
      longitude: float
      image: str | None      # Фото шоу-рума
      is_active: bool
      sort_order: int
  ```
- [ ] **Repository** `ShowroomRepository` (ABC + SqlAlchemy implementation)
- [ ] **Миграция** Alembic: таблица `showrooms`
- [ ] **API endpoint** `GET /api/showrooms` — список активных шоу-румов
  - Response: `[ShowroomSchema]`
  - Сортировка: по `sort_order`
- [ ] **Seed data** — минимум 2–3 тестовых шоу-рума

### 7.2 Frontend

**Файл:** `frontend/src/domains/content/ui/HomePage.tsx` → новая секция `ShowroomsSection`

**Изменения:**

- [ ] Создать `ShowroomsSection`:
  - Заголовок: «Приходите и потрогайте» / «Наши шоу-румы»
  - Подзаголовок: «Посмотрите панели вживую в наших шоу-румах и дилерских центрах»
  - Список карточек шоу-румов (город, адрес, часы работы, фото)
  - CTA: «Все точки на карте» → `/contacts` (или отдельная страница `/showrooms`)
- [ ] Карточка шоу-рума:
  - Фото шоу-рума (или placeholder)
  - Город (крупно) + адрес + часы работы
  - Кнопка «Как добраться» (ссылка на Яндекс.Карты по координатам)
- [ ] Данные: `useQuery` → `GET /api/showrooms`
- [ ] Горизонтальный scroll на mobile (если > 2 шоу-румов)

### 7.3 Тесты

**Backend:**
- [ ] Pytest: `GET /api/showrooms` возвращает список активных шоу-румов
- [ ] Pytest: неактивные шоу-румы не включаются в ответ
- [ ] Pytest: корректная сортировка по `sort_order`

**Frontend:**
- [ ] Unit-тест: `ShowroomsSection` рендерит карточки шоу-румов
- [ ] Unit-тест: ссылка «Как добраться» формирует URL Яндекс.Карт
- [ ] Unit-тест: skeleton при loading
- [ ] Unit-тест: если 0 шоу-румов — секция не отображается

### Критерии приёмки

- Шоу-румы загружаются с API, управляются через будущий admin panel
- Каждый шоу-рум: город, адрес, часы, координаты
- Ссылка «Как добраться» открывает карту
- Секция скрывается, если шоу-румов нет
- Responsive: горизонтальный scroll на mobile

---

## Фаза 8: Промо-баннер, подписка и преимущества — Обновление

**Цель:** Обновить `PromoBannerSection` и `AdvantagesSection` для отражения новых возможностей: подписка как основная модель, ИИ-дизайнер, шоу-румы.

### 8.1 Backend

Нет backend-изменений. Используются существующие API подписок.

### 8.2 Frontend

**Файл:** `frontend/src/domains/content/ui/HomePage.tsx` → секции `PromoBannerSection`, `AdvantagesSection`

**Изменения в `PromoBannerSection`:**

- [ ] Заменить «Скидка 15% + промокод FIRST15» на описание подписки:
  - Заголовок: «Подписка — выгодное решение»
  - Описание: ежемесячное обновление дизайна накладок, доступ к ИИ-дизайнеру, приоритетный монтаж
  - Чеклист перков: «Смена дизайна каждый месяц», «ИИ-дизайнер включён», «Приоритетный монтаж»
  - CTA: «Подробнее о подписке» → `/pricing`
  - Рядом с CTA: иконка `?` с Tooltip, объясняющим условия (текст из документа: стоимость ежемесячной подписки)
- [ ] Сохранить визуальный стиль (зелёный gradient banner, декоративные элементы)

**Изменения в `AdvantagesSection`:**

- [ ] Обновить массив `advantages` (6 карточек):
  1. **ИИ-дизайнер** — «Загрузите фото — получите персональный дизайн за минуту» (иконка: ExperimentOutlined)
  2. **Подписка** — «Меняйте дизайн каждый месяц без переплаты» (иконка: CrownOutlined)
  3. **Шоу-румы** — «Потрогайте панели вживую в наших шоу-румах» (иконка: ShopOutlined)
  4. **Быстрый монтаж** — «Монтаж за 2 часа — без пыли, без шума» (иконка: ThunderboltOutlined)
  5. **Автоматический расчёт** — «Точная стоимость за 30 секунд» (иконка: CalculatorOutlined)
  6. **Гарантия и доставка** — «5 лет гарантии. Доставка по всей России» (иконка: SafetyCertificateOutlined)
- [ ] Добавить импорты новых иконок из `@ant-design/icons`

### 8.3 Тесты

- [ ] Unit-тест: `PromoBannerSection` рендерит текст о подписке (не о промокоде)
- [ ] Unit-тест: Tooltip с условиями подписки отображается
- [ ] Unit-тест: `AdvantagesSection` рендерит 6 обновлённых карточек
- [ ] Unit-тест: каждая карточка содержит иконку, заголовок и описание
- [ ] Snapshot-тест: обновить snapshots для обеих секций

### Критерии приёмки

- Промо-баннер продвигает подписку, а не скидку
- Tooltip объясняет условия подписки
- Преимущества отражают новые фичи: ИИ, шоу-румы, подписка, автоматический расчёт
- Визуальный стиль секций не ломается

---

## Фаза 9: Отзывы, финальный CTA и интеграция

**Цель:** Обновить `ReviewsSection` и `CTABannerSection`, выполнить финальную интеграцию всех секций, проверить responsive layout и навигацию.

### 9.1 Backend

Нет backend-изменений.

### 9.2 Frontend

**Файл:** `frontend/src/domains/content/ui/HomePage.tsx` → секции `ReviewsSection`, `CTABannerSection`, `HomePage`

**Изменения в `ReviewsSection`:**

- [ ] Расширить данные отзывов: добавить поле `interior_image` (фото интерьера от клиента, опционально)
- [ ] Если есть `interior_image` — показать миниатюру над текстом отзыва
- [ ] Добавить кнопку «Все отзывы →» → `/portfolio` (или будущая страница отзывов)

**Изменения в `CTABannerSection`:**

- [ ] Обновить текст: «Готовы преобразить стены?» → «Создайте свой идеальный интерьер»
- [ ] Обновить подзаголовок: упоминание каталога, конфигуратора и шоу-румов
- [ ] Три CTA-кнопки вместо двух:
  - Основная: «Подобрать решение» → `/configurator`
  - Вторичная: «Смотреть каталог» → `/catalog`
  - Третья (outline): «Найти шоу-рум» → `/contacts`

**Финальная интеграция в `HomePage`:**

- [ ] Обновить порядок рендера секций в `HomePage` (см. «Порядок секций» в обзоре)
- [ ] Обновить обработчики навигации:
  - Добавить `handleConfigurator = () => navigate('/configurator')`
  - Добавить `handleShowrooms = () => navigate('/contacts')`
  - Обновить пропсы секций
- [ ] Обновить `PageMeta`:
  - title: «Wonder Wow Wall — дизайн-платформа для стен»
  - description: «Каталог 3D-панелей, ИИ-дизайнер, автоматический расчёт. Шоу-румы в России. Подписка от {price} ₽/мес.»
- [ ] Обновить responsive стили (`<style>` блок):
  - Добавить media-queries для новых секций (CatalogModes, Configurator, Showrooms)
  - Проверить breakpoint 768px для всех секций
- [ ] Проверить отсутствие дублирования CSS-классов между секциями

### 9.3 Тесты

**Frontend:**
- [ ] Unit-тест: `HomePage` рендерит все 11 секций в правильном порядке
- [ ] Unit-тест: все навигационные обработчики определены и вызываются
- [ ] Unit-тест: `CTABannerSection` содержит 3 кнопки с правильными маршрутами
- [ ] Unit-тест: `ReviewsSection` корректно рендерит отзывы с и без `interior_image`
- [ ] Smoke-тест: полный рендер `HomePage` без ошибок в console
- [ ] Визуальная проверка:
  - Desktop (1200px+): все секции корректно отображаются
  - Tablet (768px): responsive layout работает
  - Mobile (375px): все секции в stack, горизонтальные scroll'ы работают
- [ ] Проверка навигации: все CTA ведут на правильные маршруты

### Критерии приёмки

- Все 11 секций рендерятся в правильном порядке
- Навигация из каждой секции ведёт на корректные маршруты
- Responsive layout работает на 3 breakpoints
- Нет console errors
- PageMeta содержит актуальные SEO-данные
- Общий размер файла HomePage.tsx разумен (при необходимости — декомпозиция в отдельные файлы, но в рамках `content/ui/`)

---

## Общие замечания

### Декомпозиция файла

Текущий `HomePage.tsx` — 1665 строк. После добавления 2 новых секций и обновления существующих файл может вырасти до 2000+. Рекомендуется:

- Выделить каждую секцию в отдельный файл: `content/ui/home/HeroSection.tsx`, `content/ui/home/CatalogModesSection.tsx` и т.д.
- `HomePage.tsx` превращается в композицию импортов
- Shared-константы (ACCENT, DARK, анимации) вынести в `content/ui/home/constants.ts`
- Это не блокер и может быть сделано в любой фазе, но рекомендуется начать с Фазы 1

### Ассеты и изображения

- Текущие hero-изображения — Unsplash URL. Для production нужны реальные фото интерьеров.
- Шоу-румы, интерьерные решения, категории — требуют загруженных фото через admin panel.
- На этапе разработки допустимы placeholder'ы, но итоговая приёмка требует реальных ассетов.

### AR-режим

Документ заказчика содержит вопрос: «Можем ли мы переносить панель на стену в режиме дополненной реальности?». Это не включено в данный план, т.к. требует отдельного исследования (WebXR, ARKit, технологический стек). Если будет принято решение о реализации — создать отдельный exec-plan.

### Роли пользователей

Документ упоминает типы: Гость / Авторизованный / Партнёр / Дилер / Админ. На главной странице это не критично (контент одинаков для всех), но CTA «Рассчитать» и автосохранение спецификации в «Мои проекты» требуют проверки авторизации. Это покрыто в Фазе 6 (калькулятор) через flow: если не авторизован → окно авторизации → возврат в корзину.
